# 🌐 AI Companion 2.1 - 公网访问地址

---

## ✅ 当前可访问地址（内网）

### 📱 首页 + 完整功能
**地址**: **http://localhost:8002/**

**功能验证**:
- ✅ 角色（Luna/Max）+ 五维数值（魅力/颜值）
- ✅ 实时聊天（模拟 AI 回复 + 打字效果）
- ✅ 隐藏事件触发（LCK 驱动）- 10%
- ✅ 道具掉落（LCK 道具加成 + 稀有度）
- ✅ 背包（使用/删除 + 稀有度显示）
- ✅ 订阅系统（FREE / PRO / PREMIUM）
- ✅ 管理后台（实时数据）

**技术**: Python 3.x HTTP Server + Vanilla JS + CSS  
**端口**: 8002

---

### 📊 Demo 版本（备份）
**地址**: **http://localhost:8001/demo.html**（部分功能）

---

## 🌐 公网访问方案

### 方案 1：ngrok 内网穿透（推荐，5 分钟）

**安装 ngrok**:
```bash
# macOS
brew install ngrok

# Linux (Ubuntu/Debian)
wget https://bin.equinox.io/c/b4nFH/bin/ngrok-v3-stable-linux-amd64.zip
unzip ngrok-v3-stable-linux-amd64.zip
./ngrok authtoken <YOUR_AUTH_TOKEN>
```

**启动公网访问**:
```bash
# 配置 ngrok（只需一次）
./ngrok config add-authtoken <YOUR_AUTH_TOKEN>

# 创建到本地 8002 端口的隧道
./ngrok http 8002
```

**公网访问地址**:
```
https://xxxx-xxxx-8002.ngrok-free.app
```

**说明**:
- ✅ 免费版本：永久有效，每次启动会生成新地址
- ✅ HTTPS 支持
- ✅ 完整功能：角色 + 聊天 + 道具 + 订阅 + 后台
- ⚠️ 重启后需要执行上述命令

---

### 方案 2：部署到 VPS + 域名（生产环境）

**步骤**:
1. 购买 VPS（推荐：云服务商如阿里云/腾讯云/Vultr）
2. 安装 Nginx + 静态文件部署
3. 绑定域名 (https://your-domain.com)
4. 配置 SSL 证书

**部署命令**:
```bash
# 将 dist/index.html 复制到 VPS
scp -r /home/node/.openclaw1/projects/ai-companion/dist/* user@vps-ip:/var/www/html/

# 在 VPS 上安装 Nginx
sudo apt install nginx
sudo cp /var/www/html/dist/* /var/www/html/
sudo systemctl restart nginx
```

**公网地址**:
- https://your-domain.com

---

## 🎯 推荐使用

**开发/测试**:
- ✅ 使用 **http://localhost:8002/** （最快）

**公网展示（快速）**:
- ✅ 使用 **ngrok** + 上述命令（5 分钟）

**生产环境**:
- ✅ 部署到 VPS + 域名（长期）

---

**当前建议**:

如果你现在需要公网可访问地址，最快速的方式：

1. 安装 ngrok（见上面命令）
2. 执行 `./ngrok http 8002`
3. 使用生成的 https://xxxx-8002.ngrok-free.app 访问

这会立即提供一个公网 HTTPS 地址！

---

_生成时间: 2026-02-25 01:25 UTC_
